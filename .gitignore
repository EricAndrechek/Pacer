# Xcode
build/
Build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/
.netrc
# Package.resolved is committed — this is an app, we want reproducible builds.

# Generated Xcode project (regenerated from project.yml)
Pacer.xcodeproj/

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Sparkle
sparkle-private-key
*.priv

# Local release secrets — NEVER commit. The keys/ folder holds the
# Developer ID .p12, the App Store Connect .p8, and a .env with the
# cert password + notary IDs. The *.p12/*.p8/.env globs are belt-and-
# suspenders in case a key file ever lands outside keys/.
keys/
*.p12
*.p8
.env

# Local build artifacts
*.app
*.dmg
appcast.xml
updates/

# Editor
.vscode/
.idea/
*.swp

# Claude Code local state — ignored, EXCEPT shared project skills (checked in
# so every contributor/agent gets them, e.g. .claude/skills/ship).
.claude/*
!.claude/skills/


# Vendored DuckDB static XCFramework. Not used by main — the DuckDB work
# lives on spike/duckdb-integration (see docs/duckdb-archive.md) — but that
# build script writes here, and an 80 MB binary must never reach git history.
# Ignored defensively so running it on any branch cannot commit it.
Vendor/

Vendor/DuckDB.xcframework/
Vendor/DuckDB.xcframework.zip
